package fileAnalysis;

public class CountVowels {
    public static int countVowels(String inputString){
        if (inputString==null || inputString.isEmpty()) //Empty and null strings have no vowels
            return 0;
        else {
            int numVowels = 0;
            //finish counting algorithm here
            return numVowels;
        }


    }



}